<?xml version="1.0" encoding="UTF-8"?>
<!--
  Windows Task Scheduler definition for the Agent-Trading orchestrator.
  Runs scheduling/run_orchestrator.ps1, which activates the .venv and calls
  python orchestrator.py. The wrapper rewrites this task's trigger after each
  run from state/next_run.json so the orchestrator controls its own cadence.

  IMPORTANT: The <UserId> placeholder is replaced by register_task.ps1 with
  the current user's SID before import. The <Author> placeholder is replaced
  with the same UPN. Do not import this file as-is.

  Defaults:
    - Daily 13:30 UTC fallback trigger (UK 14:30 BST / 13:30 GMT) — re-runs
      even if a previous run failed to set state/next_run.json.
    - Boot trigger: starts the orchestrator 5 minutes after every login.
    - Settings: stop after 30 minutes; do not run on battery; allow demand-start.
-->
<Task version="1.4" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Author>{{USER_UPN}}</Author>
    <Description>Agent-Trading multi-agent paper-trading orchestrator (paper-only)</Description>
    <URI>\Agent-Trading\Orchestrator</URI>
  </RegistrationInfo>
  <Triggers>
    <CalendarTrigger>
      <StartBoundary>2026-05-10T13:30:00Z</StartBoundary>
      <Enabled>true</Enabled>
      <ScheduleByDay>
        <DaysInterval>1</DaysInterval>
      </ScheduleByDay>
    </CalendarTrigger>
    <LogonTrigger>
      <Enabled>true</Enabled>
      <Delay>PT5M</Delay>
    </LogonTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <UserId>{{USER_SID}}</UserId>
      <LogonType>InteractiveToken</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>true</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>true</StopIfGoingOnBatteries>
    <AllowHardTerminate>true</AllowHardTerminate>
    <StartWhenAvailable>true</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>true</RunOnlyIfNetworkAvailable>
    <IdleSettings>
      <StopOnIdleEnd>false</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>false</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <DisallowStartOnRemoteAppSession>false</DisallowStartOnRemoteAppSession>
    <UseUnifiedSchedulingEngine>true</UseUnifiedSchedulingEngine>
    <WakeToRun>false</WakeToRun>
    <ExecutionTimeLimit>PT30M</ExecutionTimeLimit>
    <Priority>7</Priority>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>powershell.exe</Command>
      <Arguments>-NoProfile -ExecutionPolicy Bypass -File "{{REPO_ROOT}}\scheduling\run_orchestrator.ps1"</Arguments>
      <WorkingDirectory>{{REPO_ROOT}}</WorkingDirectory>
    </Exec>
  </Actions>
</Task>
